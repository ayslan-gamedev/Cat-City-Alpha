using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using CatCity;

public class ColletableObjects : MonoBehaviour
{
    public SceneColletableObjects colletable;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
